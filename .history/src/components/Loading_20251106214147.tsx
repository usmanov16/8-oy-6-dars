
import {PropagateLoader} from "react-spinners"
function Loading() {
  return (
    <div>
        <PropagateLoader color={document.body.classList.contains("dark") ? "white" />
    </div>
  )
}

export default Loading